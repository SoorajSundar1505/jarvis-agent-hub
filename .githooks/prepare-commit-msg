#!/bin/sh
# Strip Cursor attribution trailers so cursoragent never appears on GitHub.
if [ -f "$1" ]; then
  grep -viE '^(Co-authored-by: Cursor|Made-with: Cursor|Made with Cursor)' "$1" \
    | grep -vi 'cursoragent@cursor.com' > "$1.tmp" && mv "$1.tmp" "$1"
fi
